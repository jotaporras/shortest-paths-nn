# Sparse GT + RPEARL (data embedding) for Philadelphia terrain
# Sweep-tuned hyperparameters (epochs=100, lr=0.0035, p=4)

sparse-gt-rpearl:
  gnn:
    constr:
      input: 3
      hidden: 64
      output: 64
      layers: 3
    layer_norm: false
    dropout: true
    activation: lrelu

    sparse_gt:
      hidden_dim: 64
      num_layers: 2
      num_heads: 2
      num_hops: 5
      rpearl_samples: 30
      rpearl_num_layers: 6
      pe_k: 5
      dropout: 0.015
      attn_dropout: 0.15

  mlp:
    constr:
      input: 64
      hidden: 64
      output: 1
      layers: 3
    layer_norm: false
    dropout: true
